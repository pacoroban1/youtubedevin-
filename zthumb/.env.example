# Z-Image Thumbnail Engine Configuration

# Models directory (where SDXL models are stored)
ZTHUMB_MODELS_DIR=/models

# Outputs directory (where generated images are saved)
ZTHUMB_OUTPUTS_DIR=/outputs

# Enable safe mode (blocks unsafe prompts)
ZTHUMB_SAFE_MODE=true

# Default backend (auto, turbo, full, gguf)
ZTHUMB_DEFAULT_BACKEND=auto

# Optional LoRA fine-tune (style adapter)
# Adapter path is inside the container. If you use the default volume mapping,
# place LoRA under ../outputs/lora and set:
#   Z_LORA_PATH=/outputs/lora/<adapter_dir_or_file>
#   Z_LORA_SCALE=0.8
Z_LORA_PATH=
Z_LORA_SCALE=0.8
