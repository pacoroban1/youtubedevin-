# Amharic Recap Autopilot - Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# YouTube Data API (Required)
# ===========================================
# Get these from Google Cloud Console > APIs & Services > Credentials
YOUTUBE_CLIENT_ID=your_client_id_here
YOUTUBE_CLIENT_SECRET=your_client_secret_here
YOUTUBE_REFRESH_TOKEN=your_refresh_token_here
YOUTUBE_API_KEY=your_api_key_here

# ===========================================
# Azure Speech Services (Required for TTS)
# ===========================================
# Get these from Azure Portal > Cognitive Services > Speech
AZURE_SPEECH_KEY=your_azure_speech_key_here
AZURE_SPEECH_REGION=eastus

# ===========================================
# Google Gemini API (Required for Script Generation)
# ===========================================
# Get this from Google AI Studio: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# Google Cloud Translation (Optional)
# ===========================================
# If you want a separate translation step before the Amharic rewrite/persona pass:
# 1) Enable Cloud Translation API in your Google Cloud project
# 2) Create an API key and put it here
GOOGLE_CLOUD_API_KEY=your_google_cloud_api_key_here

# Open-source alternative: LibreTranslate (self-hosted)
# Example: run a LibreTranslate server and point this URL at it.
LIBRETRANSLATE_URL=
LIBRETRANSLATE_API_KEY=

# Enable translation-driven script mode:
#   (default is Gemini rewrite in Amharic; this switches to translate-then-style)
# If GOOGLE_CLOUD_API_KEY is set and TRANSLATION_PROVIDER is empty, the runner defaults to `google`.
# Set TRANSLATION_PROVIDER=none to force-disable translation.
# Supported values:
#   - google
#   - libretranslate
#   - none
TRANSLATION_PROVIDER=
# Optional narrator persona for style pass (used when TRANSLATION_PROVIDER is enabled)
NARRATOR_PERSONA=futuristic captain
# Beat size for recap translation/stylization (seconds per beat)
SCRIPT_BEAT_SECONDS=20
SCRIPT_MAX_BEATS=60

# ===========================================
# PostgreSQL Database
# ===========================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=autopilot
POSTGRES_PASSWORD=autopilot_secret_password
POSTGRES_DB=amharic_recap

# ===========================================
# OpenAI (Optional - for DALL-E thumbnails)
# ===========================================
OPENAI_API_KEY=your_openai_api_key_here

# ===========================================
# ZThumb Local Thumbnail Engine (Optional)
# ===========================================
# If set, uses local ZThumb engine instead of OpenAI for thumbnails
# Run: cd zthumb && ./run_zthumb.sh to start the engine
# Note: if the runner is running in Docker, `localhost` inside the container is not your host.
# The runner auto-retries common host aliases when ZTHUMB_URL uses localhost.
ZTHUMB_URL=http://localhost:8100
# Paid fallback is opt-in. If ZTHUMB_URL is set and ZThumb is down, thumbnailing fails (so you can retry),
# instead of silently paying for DALL·E.
ALLOW_THUMBNAIL_FALLBACK_TO_OPENAI=false
# Optional LoRA fine-tune adapter for ZThumb (inside the ZThumb container)
Z_LORA_PATH=/outputs/lora/zthumb_lora
Z_LORA_SCALE=0.8

# ===========================================
# Telegram Distribution (Optional)
# ===========================================
# Create a bot via @BotFather and get the token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
# Get channel ID by forwarding a message to @userinfobot
TELEGRAM_CHANNEL_ID=your_channel_id_here

# ===========================================
# Twitter/X Distribution (Optional)
# ===========================================
# Get these from Twitter Developer Portal
TWITTER_API_KEY=your_twitter_api_key_here
TWITTER_API_SECRET=your_twitter_api_secret_here
TWITTER_ACCESS_TOKEN=your_twitter_access_token_here
TWITTER_ACCESS_SECRET=your_twitter_access_secret_here

# ===========================================
# n8n Configuration
# ===========================================
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change_this_password

# ===========================================
# Runner Service Configuration
# ===========================================
MEDIA_DIR=/app/media
LOG_LEVEL=INFO
